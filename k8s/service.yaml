apiVersion: v1
kind: Service
metadata:
  name: feature-context-toggle
  namespace: confuse
  labels:
    app: feature-context-toggle
    service: features
spec:
  type: ClusterIP
  selector:
    app: feature-context-toggle
  ports:
  - name: http
    port: 3099
    targetPort: 3099
    protocol: TCP
